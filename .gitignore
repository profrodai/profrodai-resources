# Agent-exhaust exclusion, per profrodai/org RULING-215 section 6a.
# This repo is public and authored by agentic tools. None of the
# directories or files below are student material, and several
# (.env*, .mcp.json) are credential-shaped. They are excluded here,
# in the initial commit, before any of them can be tracked.
.claude/
.cursor/
.agent/
.agents/
.codex/
.continue/
.windsurf/
.aider*
.github/copilot-*
CLAUDE.md
AGENTS.md
.mcp.json
.env
.env.*
.DS_Store

# Dependency and build output, standard across every course directory.
node_modules/
dist/
*.tsbuildinfo
