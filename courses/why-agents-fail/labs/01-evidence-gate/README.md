# Evidence gate

**Status:** scaffolded vertical slice

Input a claim's required and supplied source IDs. The output is a visible pass/fail decision with
any missing source IDs, rather than a confidence score.
